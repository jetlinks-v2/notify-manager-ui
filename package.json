{
  "name": "notify-manager-ui",
  "id": "notify-manager",
  "version": "1.0.0",
  "description": "JetLinks notice center UI sub module",
  "main": "index.ts",
  "scripts": {
    "dev": "pnpm --filter jetlinks-web-core dev -- --module-name notify-manager-ui",
    "build": "pnpm --filter jetlinks-web-core build -- --module-name notify-manager-ui"
  },
  "dependencies": {},
  "peerDependencies": {}
}
